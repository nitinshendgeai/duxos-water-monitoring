# Copy to .env for local development. On Railway, DATABASE_URL is injected
# automatically by the attached Postgres service — set it explicitly only
# when running outside Railway.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/duxos_water
DEFAULT_ADMIN_PIN=1234
TIMEZONE=Asia/Kolkata
CORS_ORIGINS=*
